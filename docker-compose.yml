services:
  birthday:
    build: .
    container_name: birthday-site
    restart: always
    ports:
      - "3001:80"

  backend:
    build: ./backend
    container_name: birthday-backend
    restart: always
    ports:
      - "5000:5000"